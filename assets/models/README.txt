# Place the ONNX model file here:
#   model_random_forest_opset12.onnx
#
# Copy from your project root:
#   cp /path/to/Group15-final-Year-Project/model_random_forest_opset12.onnx ./
#
# The app loads this file via InferenceEngine.init() using rootBundle.
# File must be listed in pubspec.yaml under flutter > assets > assets/models/
